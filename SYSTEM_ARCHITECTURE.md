# 🏗️ Purok Management System - Visual Architecture & Working Tree

## 📐 System Component Hierarchy

```
PUROK MANAGEMENT SYSTEM (PMS)
│
├─── 🌐 CLIENT TIER
│    │
│    ├─── Web Browsers
│    │    ├─── Desktop (Chrome, Firefox, Edge)
│    │    └─── Mobile (Responsive Design)
│    │
│    └─── User Interfaces
│         ├─── Admin Portal (Full Access)
│         ├─── Youth Portal (Limited Access)
│         └─── Guest View (Login Only)
│
├─── 🎨 PRESENTATION TIER
│    │
│    ├─── Frontend Framework
│    │    ├─── AdminLTE 3.x (Admin Template)
│    │    ├─── Bootstrap 4.x (CSS Framework)
│    │    └─── jQuery 3.x (JavaScript Library)
│    │
│    ├─── UI Components
│    │    ├─── DataTables (Table Management)
│    │    ├─── Chart.js (Data Visualization)
│    │    ├─── Select2 (Enhanced Dropdowns)
│    │    ├─── SweetAlert2 (Modal Dialogs)
│    │    ├─── Toastr (Notifications)
│    │    └─── Font Awesome (Icons)
│    │
│    └─── Pages & Views
│         ├─── Admin Pages (/admin/*.php)
│         ├─── User Pages (/user/*.php)
│         ├─── Shared Components (/inc/*.php)
│         └─── Modal Forms (Dynamic Loading)
│
├─── 💼 APPLICATION TIER
│    │
│    ├─── Core System Files
│    │    ├─── config.php (Configuration)
│    │    ├─── initialize.php (System Init)
│    │    ├─── index.php (Entry Point)
│    │    └─── .htaccess (URL Routing)
│    │
│    ├─── PHP Classes (/classes/)
│    │    │
│    │    ├─── Database Layer
│    │    │    ├─── DBConnection.php
│    │    │    └─── DBConnection_public.php
│    │    │
│    │    ├─── Authentication Layer
│    │    │    └─── Login.php
│    │    │
│    │    ├─── User Management Layer
│    │    │    ├─── Users.php
│    │    │    ├─── Users_public.php
│    │    │    └─── UsersPublic.php
│    │    │
│    │    ├─── Business Logic Layer
│    │    │    └─── Master.php
│    │    │         ├─── Event Management
│    │    │         ├─── Announcement Management
│    │    │         ├─── Attendance Management
│    │    │         ├─── Forum Management
│    │    │         └─── Statistics & Analytics
│    │    │
│    │    ├─── Utility Layer
│    │    │    ├─── QRCodeGenerator.php
│    │    │    └─── SystemSettings.php
│    │    │
│    │    └─── API Layer
│    │         └─── AJAX Endpoints (via ?f= parameter)
│    │
│    ├─── Admin Modules (/admin/)
│    │    ├─── Dashboard (home.php)
│    │    ├─── User Management (/user/)
│    │    ├─── Event Management (/event/)
│    │    ├─── Announcement Management (/announcement/)
│    │    ├─── QR Code Tools (/QRCode/)
│    │    ├─── Attendance Tracking (/attendance/)
│    │    ├─── Reports (/reports/)
│    │    ├─── SK Officials (/skofficials/)
│    │    ├─── About Us (/aboutus/)
│    │    ├─── Developers Info (/devs/)
│    │    ├─── Population Analytics (/population/)
│    │    ├─── System Settings (/system_info/)
│    │    └─── Forum Management (integrated)
│    │
│    └─── User Modules (/user/)
│         ├─── User Dashboard (home.php)
│         ├─── Profile View
│         ├─── My QR Code
│         ├─── Announcements View
│         ├─── Events View
│         ├─── Forum Participation
│         └─── Attendance History
│
├─── 🗄️ DATA TIER
│    │
│    ├─── MySQL Database (pms_db)
│    │    │
│    │    ├─── Core Tables
│    │    │    ├─── users (Youth accounts)
│    │    │    ├─── system_settings (Config)
│    │    │    └─── system_info (Metadata)
│    │    │
│    │    ├─── Content Tables
│    │    │    ├─── event_list (Events)
│    │    │    ├─── announcement_list (Announcements)
│    │    │    └─── forum_messages (Community)
│    │    │
│    │    ├─── Activity Tables
│    │    │    ├─── event_attendance (QR Scans)
│    │    │    └─── attendance (Manual Tracking)
│    │    │
│    │    └─── Legacy Tables
│    │         ├─── prison_list (Unused)
│    │         ├─── cell_list (Unused)
│    │         ├─── crime_list (Unused)
│    │         └─── inmate_list (Unused)
│    │
│    └─── File Storage
│         ├─── /uploads/announcements/ (Images)
│         ├─── /uploads/events/ (Images)
│         ├─── /uploads/users/ (Avatars)
│         └─── /uploads/inmates/ (Legacy)
│
└─── 🔌 EXTERNAL SERVICES
     │
     ├─── Camera API (HTML5 Media Capture)
     ├─── Font Awesome CDN (Icons)
     ├─── Google Fonts (Typography)
     └─── Chart.js CDN (Charts - optional)
```

---

## 🔄 Data Flow Diagram

### Complete Request-Response Cycle

```
┌─────────────┐
│   USER      │
│  (Browser)  │
└──────┬──────┘
       │
       │ 1. HTTP Request
       │    (GET/POST)
       ▼
┌──────────────────────────────┐
│    APACHE WEB SERVER         │
│    (XAMPP)                   │
└──────┬───────────────────────┘
       │
       │ 2. Route to PHP
       ▼
┌──────────────────────────────┐
│    PHP INTERPRETER           │
│    (Parse .php files)        │
└──────┬───────────────────────┘
       │
       │ 3. Load Config
       ▼
┌──────────────────────────────┐
│    config.php                │
│    initialize.php            │
│    ├─ Set timezone           │
│    ├─ Start session          │
│    ├─ Define constants       │
│    └─ Load settings          │
└──────┬───────────────────────┘
       │
       │ 4. Check Authentication
       ▼
┌──────────────────────────────┐
│    Session Check             │
│    (sess_auth.php)           │
└──────┬───────────────────────┘
       │
       ├─── Not Logged In ────────────┐
       │                              ▼
       │                     ┌─────────────────┐
       │                     │ Redirect to     │
       │                     │ login.php       │
       │                     └─────────────────┘
       │
       └─── Logged In ────────────────┐
                                      ▼
                            ┌─────────────────────┐
                            │ Load Main Page      │
                            │ (index.php)         │
                            └──────┬──────────────┘
                                   │
                                   │ 5. Check Page Parameter
                                   ▼
                            ┌─────────────────────┐
                            │ ?page=home          │
                            │ ?page=user          │
                            │ ?page=event         │
                            │ etc.                │
                            └──────┬──────────────┘
                                   │
                                   │ 6. Include Module
                                   ▼
                            ┌─────────────────────┐
                            │ Load Module File    │
                            │ (e.g., home.php)    │
                            └──────┬──────────────┘
                                   │
                                   │ 7. Render HTML + JS
                                   ▼
                            ┌─────────────────────┐
                            │ Generate Page       │
                            │ - HTML Structure    │
                            │ - CSS Styling       │
                            │ - JavaScript Code   │
                            └──────┬──────────────┘
                                   │
                                   │ 8. Send to Browser
                                   ▼
                            ┌─────────────────────┐
                            │ Browser Renders     │
                            └──────┬──────────────┘
                                   │
                                   │ 9. User Interaction
                                   ▼
                            ┌─────────────────────┐
                            │ JavaScript Event    │
                            │ (Click, Submit)     │
                            └──────┬──────────────┘
                                   │
                                   │ 10. AJAX Request
                                   ▼
                            ┌─────────────────────┐
                            │ $.ajax({            │
                            │   url: Master.php   │
                            │   data: {...}       │
                            │ })                  │
                            └──────┬──────────────┘
                                   │
                                   │ 11. Process Request
                                   ▼
                            ┌─────────────────────┐
                            │ Master.php          │
                            │ ?f=function_name    │
                            └──────┬──────────────┘
                                   │
                                   │ 12. Call Method
                                   ▼
                            ┌─────────────────────┐
                            │ Master->function()  │
                            │ - Validate input    │
                            │ - Check permissions │
                            └──────┬──────────────┘
                                   │
                                   │ 13. Database Query
                                   ▼
                            ┌─────────────────────┐
                            │ MySQL Query         │
                            │ via $this->conn     │
                            └──────┬──────────────┘
                                   │
                                   │ 14. Process Result
                                   ▼
                            ┌─────────────────────┐
                            │ Format Response     │
                            │ json_encode([       │
                            │   status: success   │
                            │   data: {...}       │
                            │ ])                  │
                            └──────┬──────────────┘
                                   │
                                   │ 15. Return JSON
                                   ▼
                            ┌─────────────────────┐
                            │ AJAX Success        │
                            │ Callback            │
                            └──────┬──────────────┘
                                   │
                                   │ 16. Update UI
                                   ▼
                            ┌─────────────────────┐
                            │ DOM Manipulation    │
                            │ - Update table      │
                            │ - Show notification │
                            │ - Refresh chart     │
                            └─────────────────────┘
```

---

## 🎯 Module Interaction Map

```
┌───────────────────────────────────────────────────────────────────┐
│                          ADMIN DASHBOARD                          │
│                           (home.php)                              │
└─────┬─────────────────────────────────────────────────────────────┘
      │
      ├─── Load Statistics ────────────────────────────────────────┐
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ AJAX Calls to Master.php                           │ │
      │   │ ├─ get_dashboard_stats (Youth, Events, Announce)   │ │
      │   │ ├─ get_gender_stats (Male/Female distribution)     │ │
      │   │ ├─ get_zone_stats (Zone population)                │ │
      │   │ ├─ get_status_stats (Active/Inactive)              │ │
      │   │ ├─ get_attendance_stats (Attendance overview)      │ │
      │   │ ├─ get_events_for_dropdown (Event selector)        │ │
      │   │ └─ get_most_attended_events (Top events)           │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                           ▼                                 │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ Charts Rendered (Chart.js)                          │ │
      │   │ ├─ Gender Doughnut Chart                            │ │
      │   │ ├─ Zone Bar Chart                                   │ │
      │   │ ├─ Status Pie Chart                                 │ │
      │   │ └─ Attendance Doughnut Chart                        │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── User Management ────────────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /admin/user/                                        │ │
      │   │ ├─ list.php (User listing with DataTables)         │ │
      │   │ ├─ manage_user.php (Create/Edit form)              │ │
      │   │ └─ Actions:                                         │ │
      │   │    ├─ Users::save() → Create/Update                │ │
      │   │    ├─ Users::delete() → Remove user                │ │
      │   │    ├─ QRCodeGenerator::generateUserQR()            │ │
      │   │    └─ Users::update_status() → Activate/Deactivate │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── Event Management ───────────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /admin/event/                                       │ │
      │   │ ├─ index.php (Event listing with cards)            │ │
      │   │ └─ Actions:                                         │ │
      │   │    ├─ Master::save_event() → Create/Update         │ │
      │   │    │  └─ Upload multiple images                    │ │
      │   │    ├─ Master::delete_event() → Remove event        │ │
      │   │    │  └─ Delete associated images                  │ │
      │   │    ├─ Master::get_all_events() → List events       │ │
      │   │    └─ Master::search_events() → Search by keyword  │ │
      │   └─────────────────────────────────────────────────────┘ │
      │             │                                               │
      │             └─── Links to QR Scanner ──────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /admin/qr_scanner.php                               │ │
      │   │ ├─ Select event from dropdown                       │ │
      │   │ ├─ Initialize camera (HTML5 getUserMedia)           │ │
      │   │ ├─ Scan QR code (JavaScript library)                │ │
      │   │ ├─ Send to Master::record_event_attendance()        │ │
      │   │ ├─ Validate QR & check duplicates                   │ │
      │   │ ├─ Insert into event_attendance table               │ │
      │   │ ├─ Play success/error sound                         │ │
      │   │ └─ Display user info or error message               │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── Announcement Management ────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /admin/announcement/                                │ │
      │   │ ├─ index.php (Announcement listing)                 │ │
      │   │ └─ Actions:                                         │ │
      │   │    ├─ Master::save_announcement()                   │ │
      │   │    │  ├─ Check duplicates (title + date)           │ │
      │   │    │  └─ Upload multiple images                    │ │
      │   │    ├─ Master::delete_announcement()                 │ │
      │   │    │  └─ Delete images from server                 │ │
      │   │    ├─ Master::get_all_announcements()               │ │
      │   │    └─ Master::search_announcements()                │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── QR Code Tools ──────────────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /admin/QRCode/                                      │ │
      │   │ ├─ modal_content.php (QR tools modal)               │ │
      │   │ ├─ Display user QR codes                            │ │
      │   │ ├─ Generate/Regenerate QR codes                     │ │
      │   │ ├─ Print QR codes                                   │ │
      │   │ └─ Download QR codes                                │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── Reports ────────────────────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /admin/reports/                                     │ │
      │   │ ├─ Youth population report                          │ │
      │   │ ├─ Event attendance report                          │ │
      │   │ ├─ Zone-wise analytics                              │ │
      │   │ ├─ Gender statistics                                │ │
      │   │ └─ Printable PDFs (future)                          │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── Forum Management ───────────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ Forum Messages (Integrated in dashboard)            │ │
      │   │ ├─ Master::send_forum_message()                     │ │
      │   │ ├─ Master::get_forum_messages()                     │ │
      │   │ │  └─ Paginated results (limit/offset)             │ │
      │   │ ├─ Master::delete_forum_message()                   │ │
      │   │ │  └─ Permission check (owner or admin)            │ │
      │   │ └─ Real-time updates via AJAX polling               │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── SK Officials ───────────────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /admin/skofficials/                                 │ │
      │   │ ├─ Manage official profiles                         │ │
      │   │ ├─ Upload photos                                    │ │
      │   │ ├─ Assign positions                                 │ │
      │   │ └─ Display on public portal                         │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── About Us & Developers ──────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /admin/aboutus/ & /admin/devs/                      │ │
      │   │ ├─ Edit about content                               │ │
      │   │ ├─ Developer information                            │ │
      │   │ └─ Display on youth portal                          │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      └─── System Settings ────────────────────────────────────────┤
                                                                    │
          ┌─────────────────────────────────────────────────────┐ │
          │ /admin/system_info/                                 │ │
          │ ├─ System name & logo                               │ │
          │ ├─ Contact information                              │ │
          │ ├─ Email settings                                   │ │
          │ └─ SystemSettings::update()                         │ │
          └─────────────────────────────────────────────────────┘ │
                                                                    │
┌───────────────────────────────────────────────────────────────────┘
│
│ YOUTH PORTAL
└───────────────────────────────────────────────────────────────────┐
      │                                                             │
      ├─── Dashboard View ──────────────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ /user/home.php                                      │ │
      │   │ ├─ Welcome message                                  │ │
      │   │ ├─ Latest announcements (3 cards)                   │ │
      │   │ ├─ Upcoming events (3 cards)                        │ │
      │   │ ├─ My QR Code display                               │ │
      │   │ ├─ SK Officials cards                               │ │
      │   │ ├─ About Us section                                 │ │
      │   │ └─ Developers info                                  │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      ├─── My Statistics ──────────────────────────────────────────┤
      │                                                             │
      │   ┌─────────────────────────────────────────────────────┐ │
      │   │ Master::get_user_statistics()                       │ │
      │   │ ├─ Total events attended                            │ │
      │   │ ├─ Attendance rate percentage                       │ │
      │   │ ├─ Recent attendance history                        │ │
      │   │ ├─ Upcoming events not attended                     │ │
      │   │ └─ Zone ranking                                     │ │
      │   └─────────────────────────────────────────────────────┘ │
      │                                                             │
      └─── Forum Participation ────────────────────────────────────┤
                                                                    │
          ┌─────────────────────────────────────────────────────┐ │
          │ Same as admin (post, view, delete own)              │ │
          └─────────────────────────────────────────────────────┘ │
                                                                    │
                                                                    │
```

---

## 🔐 Security & Authentication Flow

```
┌───────────────────────────────────────────────────────────────┐
│                    SECURITY ARCHITECTURE                      │
└───────────────────────────────────────────────────────────────┘

AUTHENTICATION PROCESS
──────────────────────

User Login Request
       │
       ▼
┌─────────────────────────────┐
│ Login.php::login()          │
│ or Login.php::user_login()  │
└──────────┬──────────────────┘
           │
           │ 1. Validate Input
           ▼
┌─────────────────────────────┐
│ Check username format       │
│ Check password not empty    │
└──────────┬──────────────────┘
           │
           │ 2. Query Database
           ▼
┌─────────────────────────────┐
│ SELECT * FROM users WHERE   │
│ username = ? AND status = 1 │
└──────────┬──────────────────┘
           │
           ├─── User Not Found ──────────┐
           │                             ▼
           │                    ┌────────────────┐
           │                    │ Return Error   │
           │                    │ "User not      │
           │                    │  found"        │
           │                    └────────────────┘
           │
           └─── User Found ──────────────┐
                                         ▼
                               ┌─────────────────┐
                               │ Verify Password │
                               │ password_verify │
                               └────────┬────────┘
                                        │
                      ┌─────────────────┴─────────────────┐
                      │                                   │
              Invalid │                            Valid  │
                      ▼                                   ▼
            ┌─────────────────┐              ┌─────────────────┐
            │ Return Error    │              │ Create Session  │
            │ "Incorrect      │              │ Variables       │
            │  password"      │              └────────┬────────┘
            └─────────────────┘                       │
                                                      │
                                                      ▼
                                           ┌─────────────────┐
                                           │ $_SESSION:      │
                                           │ - userdata      │
                                           │   - id          │
                                           │   - username    │
                                           │   - firstname   │
                                           │   - type        │
                                           │   - zone        │
                                           │ - login_type    │
                                           └────────┬────────┘
                                                    │
                                                    ▼
                                           ┌─────────────────┐
                                           │ Redirect to     │
                                           │ Dashboard       │
                                           └─────────────────┘


SESSION VERIFICATION (Every Page Load)
──────────────────────────────────────

Page Request
       │
       ▼
┌─────────────────────────────┐
│ sess_auth.php (included)    │
└──────────┬──────────────────┘
           │
           │ Check if session exists
           ▼
┌─────────────────────────────┐
│ isset($_SESSION['userdata'])│
└──────────┬──────────────────┘
           │
           ├─── Not Set ──────────────────┐
           │                              ▼
           │                     ┌────────────────┐
           │                     │ Redirect to    │
           │                     │ login.php      │
           │                     └────────────────┘
           │
           └─── Is Set ──────────────────┐
                                         ▼
                               ┌─────────────────┐
                               │ Check user type │
                               └────────┬────────┘
                                        │
                      ┌─────────────────┴─────────────────┐
                      │                                   │
              type=1  │                           type=2  │
              (Admin) │                           (Youth) │
                      ▼                                   ▼
            ┌─────────────────┐              ┌─────────────────┐
            │ Full Access     │              │ Limited Access  │
            │ All modules     │              │ User modules    │
            └─────────────────┘              └─────────────────┘


ROLE-BASED ACCESS CONTROL
──────────────────────────

Request to Admin Module
       │
       ▼
┌─────────────────────────────┐
│ Check $_SESSION['userdata'] │
│              ['type']       │
└──────────┬──────────────────┘
           │
           ├─── type != 1 (Not Admin) ────┐
           │                              ▼
           │                     ┌────────────────┐
           │                     │ 403 Forbidden  │
           │                     │ or redirect    │
           │                     └────────────────┘
           │
           └─── type == 1 (Admin) ────────┐
                                          ▼
                                ┌─────────────────┐
                                │ Grant Access    │
                                │ to Module       │
                                └─────────────────┘


QR CODE SECURITY
────────────────

QR Code Format: PMS-USER-00123-USERNAME-a1b2c3d4
                │    │      │      │         │
                │    │      │      │         └─ Hash (8 chars)
                │    │      │      └─────────── Username prefix
                │    │      └────────────────── User ID (5 digits)
                │    └───────────────────────── User type identifier
                └────────────────────────────── System prefix

Validation Process:
1. Regex pattern match
2. Extract user_id from QR
3. Query database for user with matching ID
4. Verify user.qr_code matches scanned code
5. Check user.status == 1 (active)
6. Proceed with attendance recording


INPUT SANITIZATION
──────────────────

All User Input Goes Through:
┌─────────────────────────────┐
│ 1. htmlspecialchars()       │ ← XSS Prevention
│ 2. real_escape_string()     │ ← SQL Injection Prevention
│ 3. Type casting             │ ← Data Type Validation
│ 4. Length validation        │ ← Buffer Overflow Prevention
└─────────────────────────────┘

Example:
$title = $this->conn->real_escape_string(htmlspecialchars($title));
```

---

## 📊 Database Schema Visualization

```
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE: pms_db                             │
└─────────────────────────────────────────────────────────────────┘

USERS TABLE (Central Entity)
═════════════════════════════

┌──────────────────────────────────────────────────────────────┐
│  users                                                       │
│  ┌────────────┬─────────────┬───────────────────────────┐   │
│  │ id (PK)    │ INT(11)     │ AUTO_INCREMENT           │   │
│  ├────────────┼─────────────┼───────────────────────────┤   │
│  │ firstname  │ VARCHAR(255)│                          │   │
│  │ middlename │ VARCHAR(255)│                          │   │
│  │ lastname   │ VARCHAR(255)│                          │   │
│  │ username   │ VARCHAR(255)│ UNIQUE                   │   │
│  │ password   │ TEXT        │ HASHED                   │   │
│  │ email      │ VARCHAR(255)│                          │   │
│  │ contact    │ VARCHAR(50) │                          │   │
│  │ sex        │ VARCHAR(10) │ 'Male' or 'Female'       │   │
│  │ birthdate  │ DATE        │                          │   │
│  │ zone       │ VARCHAR(50) │ Purok/Zone number        │   │
│  │ type       │ TINYINT(1)  │ 1=Admin, 2=Youth         │   │
│  │ status     │ TINYINT(1)  │ 1=Active, 0=Inactive     │   │
│  │ avatar     │ VARCHAR(255)│ Profile picture path     │   │
│  │ qr_code    │ VARCHAR(255)│ UNIQUE QR identifier     │   │
│  │ date_added │ DATETIME    │ DEFAULT CURRENT_TIMESTAMP│   │
│  └────────────┴─────────────┴───────────────────────────┘   │
└────────────┬─────────────────────────────────────────────────┘
             │
             │ (Referenced by multiple tables)
             │
        ┌────┴────┬─────────────┬───────────────────┐
        │         │             │                   │
        ▼         ▼             ▼                   ▼

┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ event_       │ │ forum_       │ │ SK Officials │ │ System Logs  │
│ attendance   │ │ messages     │ │ Assignments  │ │ (Future)     │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘


EVENT MANAGEMENT SCHEMA
═══════════════════════

┌──────────────────────────────────────────────────────────────┐
│  event_list                                                  │
│  ┌────────────┬─────────────┬───────────────────────────┐   │
│  │ id (PK)    │ INT(11)     │ AUTO_INCREMENT           │   │
│  ├────────────┼─────────────┼───────────────────────────┤   │
│  │ title      │ VARCHAR(255)│ NOT NULL                 │   │
│  │ description│ TEXT        │                          │   │
│  │ image_path │ VARCHAR(255)│ Primary image (legacy)   │   │
│  │ image_paths│ TEXT        │ JSON array of images     │   │
│  │ date_created│DATETIME    │ Event date & time        │   │
│  └────────────┴─────────────┴───────────────────────────┘   │
└────────────┬─────────────────────────────────────────────────┘
             │
             │ (One-to-Many)
             │
             ▼
┌──────────────────────────────────────────────────────────────┐
│  event_attendance                                            │
│  ┌────────────┬─────────────┬───────────────────────────┐   │
│  │ id (PK)    │ INT(11)     │ AUTO_INCREMENT           │   │
│  ├────────────┼─────────────┼───────────────────────────┤   │
│  │ event_id(FK│ INT(11)     │ → event_list.id          │   │
│  │ user_id(FK)│ INT(11)     │ → users.id               │   │
│  │ qr_code    │ VARCHAR(255)│ Scanned QR string        │   │
│  │ scan_time  │ DATETIME    │ Timestamp of scan        │   │
│  │ status     │ ENUM        │ 'present', 'absent'      │   │
│  │ scanner_   │ INT(11)     │ Admin who scanned        │   │
│  │  user_id   │             │ → users.id               │   │
│  └────────────┴─────────────┴───────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘


ANNOUNCEMENT SCHEMA
═══════════════════

┌──────────────────────────────────────────────────────────────┐
│  announcement_list                                           │
│  ┌────────────┬─────────────┬───────────────────────────┐   │
│  │ id (PK)    │ INT(11)     │ AUTO_INCREMENT           │   │
│  ├────────────┼─────────────┼───────────────────────────┤   │
│  │ title      │ VARCHAR(255)│ NOT NULL                 │   │
│  │ description│ TEXT        │                          │   │
│  │ image_path │ VARCHAR(255)│ Primary image (legacy)   │   │
│  │ images     │ TEXT        │ JSON array of images     │   │
│  │ date_created│DATETIME    │ Post date & time         │   │
│  └────────────┴─────────────┴───────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘


FORUM SCHEMA
════════════

┌──────────────────────────────────────────────────────────────┐
│  forum_messages                                              │
│  ┌────────────┬─────────────┬───────────────────────────┐   │
│  │ id (PK)    │ INT(11)     │ AUTO_INCREMENT           │   │
│  ├────────────┼─────────────┼───────────────────────────┤   │
│  │ user_id(FK)│ INT(11)     │ → users.id               │   │
│  │ message    │ TEXT        │ Message content          │   │
│  │ date_created│DATETIME    │ Post timestamp           │   │
│  └────────────┴─────────────┴───────────────────────────┘   │
└────────────┬─────────────────────────────────────────────────┘
             │
             │ (Many-to-One)
             │
             ▼
      [References users table]


SYSTEM SETTINGS SCHEMA
══════════════════════

┌──────────────────────────────────────────────────────────────┐
│  system_settings                                             │
│  ┌────────────┬─────────────┬───────────────────────────┐   │
│  │ id (PK)    │ INT(11)     │ AUTO_INCREMENT           │   │
│  ├────────────┼─────────────┼───────────────────────────┤   │
│  │ name       │ VARCHAR(255)│ System name              │   │
│  │ short_name │ VARCHAR(100)│ Short name               │   │
│  │ logo       │ VARCHAR(255)│ Logo file path           │   │
│  │ cover      │ VARCHAR(255)│ Cover image path         │   │
│  │ email      │ VARCHAR(255)│ System email             │   │
│  │ contact    │ VARCHAR(50) │ Contact number           │   │
│  │ address    │ TEXT        │ Physical address         │   │
│  │ about_     │ TEXT        │ About us content         │   │
│  │  content   │             │                          │   │
│  └────────────┴─────────────┴───────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘

RELATIONSHIPS SUMMARY
═════════════════════

users (1) ─────────── (M) event_attendance
users (1) ─────────── (M) forum_messages
event_list (1) ────── (M) event_attendance
users (scanner) (1) ─ (M) event_attendance

Legend:
PK = Primary Key
FK = Foreign Key
(1) = One
(M) = Many
```

---

**End of System Architecture Documentation**

*This working tree provides a comprehensive visual representation of the system's structure, data flow, and component interactions.*
